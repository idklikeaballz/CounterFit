<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundmain">

    <ListView
        android:id="@+id/weekly_goal_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:divider="#FFFFFF"
        android:dividerHeight="1dp"
       >
        <!-- List items are defined dynamically or through an adapter -->
    </ListView>
    <View
        android:id="@+id/viewww"
        android:layout_below="@id/weekly_goal_list"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#FFFFFF" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/updateGoalButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/viewww"
        android:background="@drawable/next_button"
        android:layout_marginTop="20dp"
        android:padding="12dp"
        android:text="Update"
        android:textColor="@color/white" />

</RelativeLayout>
