<set xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fade-In and Scale-Up Animation for ImageView -->
    <set android:interpolator="@android:anim/accelerate_decelerate_interpolator">
        <alpha
            android:duration="1000"
            android:fromAlpha="0.0"
            android:toAlpha="1.0" />

        <scale
            android:duration="1000"
            android:fromXScale="0.5"
            android:toXScale="1.0"
            android:fromYScale="0.5"
            android:toYScale="1.0"
            android:pivotX="50%"
            android:pivotY="50%" />
    </set>

    <!-- Slide-Up Animation for TextView -->
    <translate
        android:duration="1000"
        android:fromYDelta="100%"
        android:toYDelta="0%"
        android:startOffset="500" /> <!-- This offset is to start the text animation after the image animation -->
</set>
