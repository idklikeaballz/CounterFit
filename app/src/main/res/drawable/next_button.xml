<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="#1A237E" /> <!-- Pressed color -->
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <ripple android:color="#1A237E"> <!-- Ripple color -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="8dp" />
                    <solid android:color="#208BE8" /> <!-- Default color -->
                </shape>
            </item>
        </ripple>
    </item>
</selector>
